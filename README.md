# 🧺 정글캠퍼스 숙소 세탁실 현황 시스템

정글캠퍼스 숙소의 세탁기와 건조기 사용 현황을 실시간으로 확인하고 관리할 수 있는 웹 애플리케이션입니다.

## 📋 주요 기능

### 🔍 실시간 현황 조회

- 남자/여자 세탁실별 세탁기, 건조기 현황 확인
- 사용가능/사용중/점검중 상태 표시
- 실시간 타이머로 남은 시간 확인
- 다음 이용 가능 시간 안내

### ⚙️ 기계 사용 관리

- 세탁기/건조기 사용 시작/중지
- 사용자 이름, 호실 번호 등록
- 사용 시간 설정 (시간/분 단위)
- 자동 완료 처리

### 🎨 사용자 친화적 UI

- 성별 구분된 세탁실 화면
- 실시간 애니메이션 (드럼 회전 효과)
- 상태별 색상 구분 (사용가능: 파란색, 사용중: 초록색, 점검중: 노란색)
- 반응형 디자인

### 💾 데이터 지속성

- 페이지 새로고침 시에도 사용 중인 기계 상태 복원
- 실시간 타이머 자동 재시작
- 서버와 실시간 동기화

## 🚀 기술 스택

- **Frontend**: Vanilla JavaScript (ES6+ Modules)
- **UI Framework**: Bootstrap 5
- **HTTP Client**: jQuery Ajax
- **Backend API**: Python Flask
- **Database**: MongoDB

## 📁 프로젝트 구조

```
├── static
    ├── js
        ├── main.js                 # 애플리케이션 엔트리포인트
        ├── WasherState.js         # 상태 관리 (세탁기/건조기 데이터)
        ├── WasherEvent.js         # 이벤트 처리 (시작/중지 모달 등)
        ├── WasherUI.js            # UI 업데이트 함수들
        ├── request.js             # API 통신 모듈
    ├── css
        ├── Mashine.css         # 기계 디자인
        ├── style.css            # 전체 디자인
├── templates
    ├── index.html              # 메인 HTML 페이지
├── app.py                      # 서버
├── initialize.py               # 초기 데이터베이스 설정
├── requirements.txt            # 의존 라이브러리
├── status_dao.py               # 데이터 베이스 연결결
└── README.md              # 프로젝트 문서
```

## 📖 사용 방법

### 세탁기/건조기 사용 시작

1. 원하는 기계의 **"시작"** 버튼 클릭
2. 모달에서 사용자 정보 입력:
   - 이름
   - 호실 번호
   - 사용 시간 (시간/분)
3. **"시작"** 버튼으로 사용 시작

### 사용 중지

1. 사용 중인 기계의 **"중지"** 버튼 클릭
2. 확인 모달에서 **"중단"** 버튼으로 확인

### 성별 구분 세탁실 전환

- 상단의 **"남자 세탁실"** / **"여자 세탁실"** 버튼으로 전환

## 🔄 API 엔드포인트

### GET `/refresh?sex={성별}`

- 세탁실 현황 조회
- 파라미터: `sex` (남자/여자)

### POST `/washers`

- 세탁기 사용자 정보 업데이트
- Body: `{ id, name, roomNum, remain }`

### POST `/dryers`

- 건조기 사용자 정보 업데이트
- Body: `{ id, name, roomNum, remain }`

### PATCH `/washers`

- 세탁기 상태 변경
- Body: `{ id, state }`

### PATCH `/dryers`

- 건조기 상태 변경
- Body: `{ id, state }`

## 🏗️ 아키텍처

### 모듈 구조

```javascript
main.js
├── WasherState.js    # 상태 관리
├── WasherEvent.js    # 이벤트 핸들링
├── WasherUI.js       # UI 렌더링
└── request.js        # API 통신
```

### 데이터 흐름

1. **초기화**: `initMachineData()` → 서버 데이터 로드
2. **UI 렌더링**: `renderMachineSets()` → DOM 생성
3. **상태 복원**: `restoreRunningMachinesOnly()` → 사용 중인 기계 복원
4. **실시간 업데이트**: `updateTimerDisplay()` → 1분마다 타이머 감소

## 🐛 알려진 이슈

- 페이지 첫 로딩 시 진행 상황 복구 실패 이슈
- 페이지 새로고침 시 바로 진행 상황 복구 실패 이슈
- 남자 여자 탭 이동 시 이름, 호실, 머신 애니메이션, 중지버튼 출력 오류
- 페이지 첫 로딩 시 DOM 렌더링 타이밍 이슈 (해결됨)
- 순환 참조로 인한 모듈 로딩 문제 (해결됨)

## 👥 개발팀

- **아이디어 제공** : 박미소
- **Full-Stack Developer**: 박미소, 임동건, 조건희
